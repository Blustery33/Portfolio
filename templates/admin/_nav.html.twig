<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">{#{{ app.user.username }}#}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('project') }}">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_new_project') }}" style="margin-left: 100px">New Project</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_all_project') }}" style="margin-left: 100px">All Projects</a>
            </li>
            <li class="nav-item">
               {# <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>#}
            </li>
        </ul>
    </div>
</nav>
